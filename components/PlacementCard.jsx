import React from 'react';
import styles from '../styles/PlacementCard.module.css';

export default function PlacementCard({}) {
    return (
        <div className={styles.wrapper}>

        </div>
    )
}